import "./box.css"

const Main = ({ children }) => {
    return (
        <main className="container_bootstrap main-box px-5">
            {children}
        </main>
    )
}

export default Main
