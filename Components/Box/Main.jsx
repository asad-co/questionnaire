import "./box.css"

const Main = ({ children }) => {
    return (
        <main className="container_bootstrap main-box ">
            {children}
        </main>
    )
}

export default Main
